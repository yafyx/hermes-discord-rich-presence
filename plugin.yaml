name: discord-rich-presence
version: "0.3.0"
description: "Hermes Discord Rich Presence plugin with customizable day, week, and month activity rotations from cached Hermes state. Uses supported plugin hooks only and does not patch Hermes core."
author: "yafyx"
provides_hooks:
  - pre_gateway_dispatch
