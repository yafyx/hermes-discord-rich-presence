name: discord-rich-presence
version: "0.2.1"
description: "Hermes Discord Rich Presence plugin that rotates recent session, message, model, and activity status from cached Hermes state. Uses supported plugin hooks only and does not patch Hermes core."
author: "yafyx"
provides_hooks:
  - pre_gateway_dispatch
